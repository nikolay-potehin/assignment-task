import 'package:flutter/material.dart';

class ScenariousPage extends StatelessWidget {
  const ScenariousPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Сценарии',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
